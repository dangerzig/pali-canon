# Pāli Canon library - core (standard library only)
# No required dependencies for basic functionality

# Optional dependencies for analysis features:
# pandas          # DataFrame export, vocabulary tables
# scipy           # Sparse matrices for document-term matrix
# numpy           # Numerical operations
